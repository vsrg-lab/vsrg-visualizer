/** Extensions the file picker offers; the parser sniffs the actual format from the bytes. */
export const CHART_FILE_ACCEPT = ".urc,.osu,.qua,.sm,.ssc,.bms,.bme,.bml,.pms,.ojn";

/** The same extensions grouped the way the empty state lists them. */
export const CHART_FILE_GROUPS = [
	".urc",
	".osu",
	".qua",
	".sm / .ssc",
	".bms / .bme / .bml / .pms",
	".ojn"
];
